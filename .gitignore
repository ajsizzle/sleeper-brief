# players_trim.json is intentionally committed: it is how the 24-hour
# player cache survives between ephemeral runners, and the workflow's
# leak scan checks it alongside the generated files.

# Python
__pycache__/
*.py[cod]

# macOS
.DS_Store
